# Uncomment the next line to define a global platform for your project
# platform :ios, '10.0'

target 'PinterstLikeCollectionViewAndFlickrImageSearch' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PinterstLikeCollectionViewAndFlickrImageSearch
  pod 'SVProgressHUD', '2.1.2'
  
  target 'PinterstLikeCollectionViewAndFlickrImageSearchTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
